@model XSystem.XReflection.XMethodInfo

@{
    Util.XToViewData(Model, ViewData);
    Util.XToAccessKeyMap(Model.XGetParameters(), ViewData);
}
@foreach (var xparameter in Model.XGetParameters())
{
    @await Html.PartialAsync("_XParameterInfo", xparameter);
}