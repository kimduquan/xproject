@model XSystem.XReflection.XMethodInfo

@{
    XUtil.XToViewData(Model, ViewData);
    XUtil.XToAccessKeyMap(Model.XGetParameters(), ViewData);
}
@foreach (var xparameter in Model.XGetParameters())
{
    @await Html.PartialAsync("_XSystem/_XReflection/_XParameterInfo", xparameter);
    <br />
}